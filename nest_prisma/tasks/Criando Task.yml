info:
  name: Criando Task
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_url}}/tasks"
  body:
    type: json
    data: |-
      {
        "name": "valida campo",
        "description": "teste de validação de campo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
